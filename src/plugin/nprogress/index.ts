import type { App } from 'vue';
import nprogress from 'nprogress';
import 'nprogress/nprogress.css';
export const setupNprogress = (app: App) => {
  // app.use(nprogress)
};

export const Nprogress = nprogress;
